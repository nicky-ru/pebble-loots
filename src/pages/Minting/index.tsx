import React from 'react';
import { observer } from 'mobx-react-lite';
import { Container } from '@chakra-ui/react';

export const Minting = observer(() => {
  return <Container>Here i will mint</Container>;
});
